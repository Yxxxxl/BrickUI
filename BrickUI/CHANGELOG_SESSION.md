1.新增 SideMenu 控件与 MVVM 数据模型，支持外部控制菜单项/图标/命令/选中态。
2.新增左侧菜单布局与右侧内容压缩/恢复逻辑，支持展开/折叠。
3.菜单项选中样式（左侧高亮）、悬停手型与加粗、行间距加大。
4.添加展开/折叠宽度动画与缓动效果，左侧单向折叠并裁剪避免溢出。
5.折叠后菜单占位为 0，右侧内容占满；操作按钮浮层显示。
6.操作按钮显示文案随状态变化，悬停改为底部描边高亮。
7.修复 XAML 中 Style.Triggers 放置错误导致的编译错误。
8.操作按钮背景/前景开放接口，支持折叠态与展开态不同颜色。
9.优化按钮颜色切换：从“延迟切色”改为与动画同步渐变（ColorAnimation）。
10.Demo 中引入并使用 MahApps.Metro.IconPacks 图标（Material）。
11.新增 demo 视图/视图模型并在主窗口展示；Demo 中设置折叠态按钮颜色与右侧背景一致。
